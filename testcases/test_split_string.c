/*
int main(void)
{
        char **store;
        char *str = NULL;
        size_t n = 0;
        char *del = " ";

        printf("enter a number: ");
        getline(&str,&n,stdin); */
        /*captureing input with getline*/
        /*store = split_string(str,del);

        while(*store)
        {
                printf("%s\n",*store);
                store++;
        }
        if (*store == NULL)
        {
                printf("The last Number is NULL\n");
                }

        return (0);
}
*/
